<h1>Badges</h1>
<table class="table">
	<% @badges.each do |badge| %>
	 	<tr>
	 		<td><%= badge.name %></td>
			<td>
			<% badge.players.each do |player| %>
			<%= image_tag player.gravatar_url %>
			<% end %>
			</td>
		</tr>
	<% end %>
</table>
